name: Overview about all project

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello: 
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "hello world"
        shell: bash

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: bye
        run: echo "good bye"
        shell: bash